/// <reference types="react" />
import { AssetRow } from "nft/types";
export declare const ContentRow: ({ row, isCollectionApprovalSection, removeRow, }: {
    row: AssetRow;
    isCollectionApprovalSection: boolean;
    removeRow: (row: AssetRow) => void;
}) => JSX.Element;
